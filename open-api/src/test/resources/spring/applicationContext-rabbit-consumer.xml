<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                http://www.springframework.org/schema/rabbit
                http://www.springframework.org/schema/rabbit/spring-rabbit-1.4.xsd
                http://www.springframework.org/schema/task  
    http://www.springframework.org/schema/task/spring-task-4.1.xsd">


	<!-- 连接服务配置 -->
	<rabbit:connection-factory id="openConnectionFactory"
		host="${open.rabbitmq.host}" username="${open.rabbitmq.username}"
		password="${open.rabbitmq.password}" port="${open.rabbitmq.port}"
		virtual-host="${open.rabbitmq.vhost}" channel-cache-size="${open.rabbitmq.channelCacheSize}" />

	<rabbit:admin connection-factory="openConnectionFactory" />

	<!-- queue 队列声明 -->
	<rabbit:queue durable="true" auto-delete="false"
		exclusive="false" name="${open.rabbitmq.queue}" />


	<!-- exchange queue binging key 绑定 -->
	<rabbit:topic-exchange name="${open.rabbitmq.exchange}"
		durable="true" auto-delete="false">
		<rabbit:bindings>
			<rabbit:binding queue="${open.rabbitmq.queue}"
				pattern="open.*.*.*" />
		</rabbit:bindings>
	</rabbit:topic-exchange>


    
    
    <!-- 异步的线程池，线程池的最在数不能设定太小，不然<rabbit:listener/>/@RabbitListener太多的话，会出现发无法正常消费问题 -->  
    <task:executor id="taskExecutor" pool-size="4-256" keep-alive="30000" queue-capacity="128" />  
	<rabbit:listener-container
		connection-factory="openConnectionFactory" message-converter="fastjsonMessageConverter"
		auto-startup="true" error-handler="" acknowledge="auto" task-executor="taskExecutor" >
		<rabbit:listener ref="queueListenter"
			queue-names="${open.rabbitmq.queue}" />
	</rabbit:listener-container>
	<bean id="queueListenter" class="com.zizaike.open.QueueListenter" />
	<bean id="fastjsonMessageConverter" class="com.zizaike.core.common.util.FastJsonMessageConverter" />
    
</beans>